# Production requirements - API-only footprint for Fly.io
# ML/vector/local-model stack is intentionally split into requirements-ml.txt.

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic[email]>=2.10.0
email-validator>=2.2.0
pydantic-settings>=2.6.0
python-multipart>=0.0.9

# Auth & Security
PyJWT>=2.9.0
bcrypt>=4.2.0
cryptography>=44.0.0

# HTTP Clients
httpx>=0.28.0
aiohttp>=3.11.0

# Database
python-dotenv>=1.1.0
sqlalchemy>=2.0.40
alembic>=1.16.0
psycopg2-binary>=2.9.10
supabase>=1.0.0

# Redis (for caching, rate limiting)
redis[hiredis]>=5.2.0

# LLM Providers (using API-based embeddings instead of local models)
openai>=1.50.0
anthropic>=0.35.0
google-generativeai>=0.5.0

# Scheduling
APScheduler>=3.11.0
psutil>=6.2.0

# Monitoring
sentry-sdk[fastapi]>=2.1.0
structlog>=24.1.0
python-json-logger>=3.0.0
prometheus-client>=0.19.0

# Retry logic
backoff>=2.0.0
tenacity>=8.2.0

# Google Drive integration (lightweight)
google-api-python-client>=2.170.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# HashiCorp Vault client
hvac>=2.0.0
